// Text-to-Speech via Silero (local, open source, natural Russian voice)

export class SileroTTS {
  async synthesize(text: string): Promise<Buffer> {
    // TODO: call local Silero model, return audio buffer
    throw new Error('Not implemented yet');
  }
}
